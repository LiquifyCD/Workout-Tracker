{
  "name": "divinity-workout-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check core.js && node --check app.js",
    "test": "npm run check && html-validate index.html && node --test"
  },
  "devDependencies": {
    "html-validate": "11.5.6"
  },
  "engines": {
    "node": ">=20"
  }
}
